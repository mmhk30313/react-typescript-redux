import { request } from "../request"

export const get_user_length = (url: string, params: any) => {
    return request(url, params);
}

export const get_post_length = (url: string, params: any) => {
    return request(url, params);
}

export const get_comment_length = (url: string, params: any) => {
    return request(url, params);
}

export const get_album_length = (url: string, params: any) => {
    return request(url, params);
}

export const get_photo_length = (url: string, params: any) => {
    return request(url, params);
}

export const get_todo_length = (url: string, params: any) => {
    return request(url, params);
}

